import 'package:flutter/material.dart';

class AppColors {
  AppColors._(); // evita instancias

  static const primary = Color(0xFF2563EB); // para acciones principales (botones, links activos, elementos destacados)
  static const primaryLight = Color(0xFF93C5FD); // hover / estados suaves

  static const background = Color(0xFFF8FAFC);
  static const surface = Color(0xFFFFFFFF); // cards, formularios

  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);

  static const error = Color(0xFFDC2626);
}